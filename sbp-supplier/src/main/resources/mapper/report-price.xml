<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wq.sbp.dao.ReportPriceDao">
 	
	<select id="selectInsuranceInfoByInsIdAndSupId" resultType="Quote">
		SELECT
			iii.`name`,
			iii.id insInfoId
		FROM
			ibs_report_price irp
		LEFT JOIN ibs_insurance_info iii ON irp.ins_info_id = iii.id
		WHERE
			irp.ins_id = #{insId}
		AND irp.member_id = #{memberId}
	</select> 	
</mapper>